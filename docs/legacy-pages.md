# Legacy and Duplicate Page Notes

## Purpose

This document tracks older or duplicate page files that may be candidates for cleanup later.

No files should be deleted just because they appear in this list.

## Confirmed Duplicate Groups from Audit

These groups currently have both a flat `.html` file and a clean folder URL version.

### Banking and Financial Services

- `banking-financial-services.html`
- `banking-financial-services/index.html`

Preferred canonical pattern:

- `/banking-financial-services`

### Capabilities

- `Capabilities.html`
- `capabilities/index.html`

Preferred canonical pattern:

- `/capabilities`

Note: desktop navbar parent label is now a dropdown trigger only, so the parent page may not be needed as a primary navigation destination.

### Celsior AI Lab

- `celsior-ai-lab.html`
- `celsior-ai-lab/index.html`

Preferred canonical pattern:

- `/celsior-ai-lab`

### Design Lab

- `design-lab.html`
- `design-lab/index.html`

Preferred canonical pattern:

- `/design-lab`

### Frameworks and Accelerators

- `frameworks-accelerators.html`
- `frameworks-accelerators/index.html`

Preferred canonical pattern:

- `/frameworks-accelerators`

### Our Focus

- `Our_Focus.html`
- `our-focus/index.html`

Preferred canonical pattern:

- `/our-focus`

Note: desktop navbar parent label is now a dropdown trigger only, so the parent page may not be needed as a primary navigation destination.

### Solutions

- `Solutions.html`
- `solutions/index.html`

Preferred canonical pattern:

- `/solutions`

Note: desktop navbar parent label is now a dropdown trigger only, so the parent page may not be needed as a primary navigation destination.

## Cleanup Decision Pending

Before removing or redirecting any of these files, confirm:

- Whether Vercel clean URLs serve the folder version
- Whether any internal links still reference the flat version
- Whether any external campaigns, HubSpot links, or sales links use the flat version
- Whether each page is needed as a standalone landing page
